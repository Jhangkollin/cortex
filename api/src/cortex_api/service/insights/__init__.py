"""Insights read-side primitives.

This package is intentionally persona-neutral. Brand and publisher routes can
project these models differently, but common metric/funnel/query behavior
belongs here instead of being copied per persona.
"""
