"use client";
// zh sibling — copy mirrors with translated affordances; for chunk 3 the shape
// is identical and the visible strings are en. We'll fork copy when a
// dedicated zh layout requires divergence.
export { PortfolioBand, type PortfolioBandProps } from "../onboarding-v2/portfolio-band";
