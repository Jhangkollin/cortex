# publisher-dashboard

**PLACEHOLDER** — components for `web/src/app/publisher/dashboard/page.tsx`.

Publisher dashboard should reuse shared dashboard primitives where the shape
matches Brand:
- KPI cards
- time range filter
- metric breakdown table
- funnel components

Do not copy the Brand dashboard wholesale. Define the Publisher metric catalog
and default breakdown dimension with Product/Data first, then extract shared UI
from `brand-dashboard/` when the second consumer appears.

Add files when the Publisher Dashboard projection begins. Until then, this
directory holds only this README.
