/**
 * Org switcher — dropdown for users with multiple memberships.
 *
 * PLACEHOLDER. At MVP, almost all users belong to one org. This component
 * activates when N > 1 memberships are detected.
 */
export function OrgSwitcher() {
  return null;
}
