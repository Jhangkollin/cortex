/**
 * Faint 36px crosshatch grid used as background texture on interior report
 * pages. Light Edition: warm-gray hairlines on cream paper (was white-on-dark
 * before the dark→light flip). The radial mask the cover uses is applied at
 * the call site, not here, so this stays a generic surface texture.
 */
export function GridBG() {
  return null;
}
